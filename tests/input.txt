let input_file = match read_file(data_1) {
    Ok(contents) => {
        println!("File contents:\n{}", contents);
        contents
    }
    Err(error) => {
        eprintln!("Error reading file: {}", error);
        return; // or handle the error in an appropriate way
    }
};
